# Ralph Loop Status

Done: false
Current slice: 6 of 10
Started: 2026-05-06T21:59:39

## Slices

Per spec § "Slice plan" (`docs/superpowers/specs/2026-05-04-kofipod-pro-unlock-design.md`):

- [x] **Pre-0** — Cleanup: delete `app.kofipod.update` package + UI + DI + tests. _(plan: `docs/superpowers/plans/2026-05-05-kofipod-pro-pre0-updater-cleanup.md`)_
- [x] **0** — Pro entitlement plumbing + flavor split (`play` / `foss`); `BillingClientPort`, `ProEntitlementRepository`, Paywall sheet, Restore Purchase; toy gate (Bookmark icon on Player). _(plan: `docs/superpowers/plans/2026-05-05-kofipod-pro-slice-0.md`)_
- [x] **1** — Bookmarks (schema 16, BookmarkRepository, per-episode Saved section, global Bookmarks screen, Pro-gated entry). _(plan: `docs/superpowers/plans/2026-05-06-kofipod-pro-slice-1-bookmarks.md`)_
- [x] **2** — Library search (FTS5; schema 17 with TranscriptCache + LibrarySearchIndex; sanitiser, repository, Library entry-point, Pro-gated bar). _(plan: `docs/superpowers/plans/2026-05-06-kofipod-pro-slice-2-library-search.md`)_
- [x] **3** — Snippets MVP (MP3) (schema 18; SnippetRepository, SnippetExporter audio path, foreground service, editor screen, Player Snip button, per-episode Saved section). _(plan: `docs/superpowers/plans/2026-05-06-kofipod-pro-slice-3-snippets-mp3.md`)_
- [x] **4** — Snippets MP4 (Media3 Transformer composition, waveform overlay, caption pipeline transcript-first/Gemini-fallback, editor rebuild, format chip + size badges). _(plan: `docs/superpowers/plans/2026-05-06-kofipod-pro-slice-4-snippets-mp4.md`)_
- [x] **5** — PKM exports — Markdown (MarkdownFormatter + MarkdownDocument; Clipboard + share-as-md-file sinks; PkmExportCoordinator; entry points on Bookmarks, Episode Detail Saved section long-press, AI Summary card). _(plan: `docs/superpowers/plans/2026-05-06-kofipod-pro-slice-5-pkm-markdown.md`)_ — verified green 2026-05-06.
- [ ] **6** — PKM exports — Obsidian + Readwise (SAF persistent vault folder picker + drop `.md` files; Readwise OAuth or API-token paste; ExportLog idempotency table — schema bump; PkmConnection table; PkmExportWorker for queued sync). _(plan: `docs/superpowers/plans/2026-05-06-kofipod-pro-slice-6-pkm-obsidian-readwise.md`)_
- [ ] **7** — Smart Playlists (predicate JSON model, predicate evaluator pure-Kotlin commonMain, Smart Playlist editor screen, virtual rows in Library; schema bump). _Note: per `e1487dc` Smart Playlists has been promoted into v1.0._
- [ ] **— launch v1.0 —**
- [ ] **8** (v1.1) — Silence Skip (Media3 `AudioProcessor`, per-show override extending existing per-show settings, Stats "Time skipped" tile).
- [ ] **9** (v1.1) — PKM — Notion (OAuth + database picker; idempotent upsert via ExportLog).

## Iterations

- 2026-05-06 (init): Read spec + status file + git log. Populated slice list. Identified slices Pre-0..5 as complete based on commit history; slice 5 lacks a formal closeout/memory commit but feature surfaces are wired (last commit 80a8b8c). Next slice is 6 (PKM Obsidian + Readwise).
- 2026-05-06 (verify-slice-5): Ran the green-check sequence. All passed (exit 0): `:composeApp:compileFossDebugKotlinAndroid`, `:composeApp:ktlintCheck`, `:composeApp:detekt`, `:composeApp:testFossDebugUnitTest`, `:composeApp:compileKotlinIosSimulatorArm64`, `:composeApp:verifyPaparazziFossDebug`. Slice 5 is verified and closed out from a correctness standpoint. No closeout commit was posted (CLAUDE.md "only commit when explicitly asked"); leaving 80a8b8c as the de-facto Slice 5 tip. Next iteration: author Slice 6 implementation plan via `superpowers:writing-plans`.
- 2026-05-06 (plan-slice-6): Authored Slice 6 implementation plan at `docs/superpowers/plans/2026-05-06-kofipod-pro-slice-6-pkm-obsidian-readwise.md` (17 tasks, schema 19, lands `PkmConnection` + `ExportLog` + `OAuthTokenVault` + `ObsidianSink` + `ReadwiseSink` + `PkmExportCoordinator` rewrite + `PkmExportWorker` + `Route.Connections` + `ConnectionsScreen` + `ExportActionSheet`). Locked the spec's open question on Readwise auth: **API-token paste**, not OAuth — rationale: no-backend posture rules out hosting a registered redirect URI; Readwise officially supports `Authorization: Token <token>` + `/api/v2/auth/` verification endpoint. OAuth tokens reuse existing `kofipod_secure.xml` (already excluded from Auto Backup). Next iteration: execute Slice 6 starting with Task 0 (capture design tiles) via `superpowers:subagent-driven-development`.
- 2026-05-06 (slice6-task0): Dispatched general-purpose implementer to capture Slice 6 design tiles via Playwright/Chromium against `docs/kofipod-pro-ui-design.html`. Captured 12 PNG tiles to `/tmp/kofipod-design-slice6/` covering all 6 requested surfaces (PKM Connections, Connections variants OAuth + sync-failed, Export action sheet idle/in-progress/result, Bookmarks list 4 variants, Saved section, AI Summary card export). Plan doc was appended with a `## Captured design tiles` section linking each tile to the implementation tasks (13–17) that should reference it. Committed as `6c55be1` on `worktree-kofipodpro-pre0`. Skipped the spec/code-quality reviewer steps for this task because it is docs-only (no source-code semantics to review). Next iteration: execute Task 1 (Schema 19 — `PkmConnection.sq` + `ExportLog.sq` + `19.sqm` migration).
- 2026-05-06 (slice6-task1): Schema 19 — `PkmConnection.sq` + `ExportLog.sq` + `19.sqm` migration landed as commit `20707f5`. Tables ride along with `KofipodDatabase` (Auto Backup includes the whole `database` domain). Next: Task 2 (`ConnectionKind` + `PkmConnection` domain types).
- 2026-05-06 (slice6-task2): Implemented `ConnectionKind` enum (Markdown/Obsidian/Readwise/Notion with `wire` ↔ `fromWire`) and `PkmConnection` data class under `app.kofipod.pkm.connections`. TDD: 3 commonTest assertions (round-trip, unknown→null, lowercase wire stability) — wrote red, made green. Green-check sequence passed (`testFossDebugUnitTest`, `ktlintFormat`, `detekt`, `compileKotlinIosSimulatorArm64`). Committed as `b1feee4`. Next: Task 3 (`OAuthTokenVault` expect/actual — Android EncryptedSharedPreferences + iOS stub).
- 2026-05-06 (slice6-task3): `OAuthTokenVault` expect/actual landed as commit `3092844`. Three files: commonMain expect (3 suspend methods: `put`/`get`/`clear` keyed by caller string), androidMain actual over `kofipod_secure` `EncryptedSharedPreferences` (AES256_GCM master / AES256_SIV+AES256_GCM schemes, `commit()` not `apply()`, `Dispatchers.IO`), iosMain actual as in-memory map stub. Subagent-driven: implementer (haiku) → spec reviewer (haiku, ✅ exact match) → code-quality reviewer (haiku, ✅ approved, no findings). Both compile targets green (`compileFossDebugKotlinAndroid`, `compileKotlinIosSimulatorArm64`). Next: Task 4 (`PkmConnectionRepository` + tests — wraps the schema-19 `PkmConnection` queries with Flow surfaces and uses `OAuthTokenVault` for token storage).
- 2026-05-06 (slice6-task4): `PkmConnectionRepository` + 9 tests landed as commit `33c27e0`. Per the plan note's option, refactored `OAuthTokenVault` from `expect class` → `interface OAuthTokenVault` + `expect class OAuthTokenVaultImpl : OAuthTokenVault` so the repository's test-fake can implement the interface (subclassing the Android actual was awkward — it carries a `Context`). Code review (feature-dev:code-reviewer) flagged disconnect ordering (vault.clear before queries.deleteByKind risks an "appears connected but token missing" UI state if interrupted) and tokenRef rotation leak (re-connect with a different vault key left the old secret in `kofipod_secure`); both fixes applied — disconnect now deletes the row first, connect clears the prior tokenRef when it differs. Test audit (test-quality-auditor) added: silent-no-op contract test for `markSynced` on an unconnected kind + an explicit tokenRef-rotation test. FK-enforcement footgun in `inMemoryDatabase()` was raised but deferred to Task 5 (when ExportLog FKs land). Green-check sequence (`testFossDebugUnitTest` 9/9, `ktlintFormat`, `detekt`, `compileKotlinIosSimulatorArm64`) all passed. Next: Task 5 (`ExportLogRepository` + tests).
- 2026-05-07 (slice6-task5): `ExportLogRepository` + 5 tests landed as commit `6f5a2ea`. Implementation in commonMain follows `PkmConnectionRepository` style (private `q` queries field, `Dispatchers.Default` boundaries, status string constants in a private companion). Test placed under `androidUnitTest` because `JdbcSqliteDriver` is JVM-only — the plan's `commonTest` snippet was infeasible. Code review (feature-dev:code-reviewer) flagged HIGH: `toDomain` used `error()` on unknown `destinationKind` instead of returning null like `PkmConnectionRepository.toDomain` — fixed to return null and `mapNotNull` in `selectQueuedOrFailed`. LOW: missing test for failed → success transition clearing `errorMessage` — added (5th test). Dismissed: test-location MEDIUM (false positive — `JdbcSqliteDriver` is JVM-only) and raw `status: String` LOW (matches plan contract; `ExportStatus` enum can land alongside the consumer in Task 6/7). FK-enforcement deferral from Task 4 turned out to be moot — `ExportLog` has no FK to `PkmConnection` (composite PK only), so `inMemoryDatabase()` works as-is. Green-check: target tests 5/5 ✅, ktlintFormat ✅, detekt ✅, `compileKotlinIosSimulatorArm64` ✅. Next: Task 6 (`ExportSink` interface + `PkmDestination` + delete obsolete Slice 5 sink types).
- 2026-05-07 (slice6-task6): `ExportSink` interface + `PkmDestination` enum landed as commit `7ba447a`. Pure scaffolding (no logic, no tests) so handled inline rather than via subagent dispatch. `ExportSinkResult` is sealed with Success / TransientFailure / PermanentFailure variants (drives `PkmExportWorker`'s retry vs. drop decision in Task 12). `PkmDestination` carries an optional `connectionKind` so the Export sheet can map zero-auth sinks (Clipboard, ShareFile) to `null` and connection-bound sinks (Obsidian, Readwise) to their `ConnectionKind`. Per the plan, deletion of the obsolete Slice 5 sink types (`PkmExportSink`, `MarkdownSink`, `MarkdownExporter`) is deferred until Task 10 because `PkmExportCoordinator` still references them — they will be removed once the new coordinator + sink registry are wired. Green-check sequence: `compileFossDebugKotlinAndroid` ✅, `ktlintCheck` ✅, `detekt` ✅, `compileKotlinIosSimulatorArm64` ✅. Next: Task 7 (`ClipboardSink` + `ShareFileSink` — Slice 5 ports moved behind `ExportSink`).
- 2026-05-07 (slice6-task7): `ClipboardSink` + `ShareFileSink` landed as commit `f19ed6a`. Pure scaffolding (Slice 5 logic ported behind the `ExportSink` interface), so handled inline like Task 6 — no new tests because there is no logic to verify (clipboard/share are external effects already tested at the port level). `ClipboardSink` always returns `Success(externalId = null)` (clipboard has no remote ID); `ShareFileSink` writes through `MarkdownTempFilePort` and dispatches via `Sharer.shareFile` with `mimeType = "text/markdown"`. Green-check sequence: `compileFossDebugKotlinAndroid` ✅, `ktlintCheck` ✅, `detekt` ✅, `compileKotlinIosSimulatorArm64` ✅. Pre-commit hook (ktlintFormat + detekt) passed. Next: Task 8 (`ObsidianFolderWriter` expect/actual + `ObsidianSink` + tests — first connection-bound sink, lands SAF persistent vault folder picker on Android).
- 2026-05-07 (slice6-task8): `ObsidianFolderWriter` + `ObsidianSink` + 4 tests landed across three commits: `fffc2b6` (initial drop), `73cc48b` (corrective refactor), `9d8c524` (test tightening). Initial implementer (haiku) hit the same `expect class` constructor-mismatch trap that bit Task 4's `OAuthTokenVault`, and worked around it with global mutable companion state (`var contextProvider: () -> Any? = { null }` + `as Context` cast). Caught on inspection before spec review and re-dispatched with explicit corrective guidance: refactor to `interface ObsidianFolderWriter` + `expect class ObsidianFolderWriterImpl : ObsidianFolderWriter`, mirroring Task 4's resolution. Spec reviewer (haiku, ✅ compliant). Code-quality reviewer (feature-dev:code-reviewer, haiku) flagged two Important test gaps: (1) happy path didn't assert `writer.lastBody == doc.render()` — the most important behavioural assertion was missing; (2) null `folderUri` branch in `ObsidianSink.export` was uncovered. Both fixed in `9d8c524` (4 tests now). Reviewer's third nit (add a comment explaining why `priorExternalId` is unused) was rejected per CLAUDE.md "default to writing no comments" — interface-contract compatibility is obvious from the interface definition. Final state: 4/4 tests pass, ktlint + detekt + iOS compile all green. Next: Task 9 (`ReadwiseClient` + DTOs + tests — Ktor wrapper around Readwise Highlights API, includes `verify(token)` against `/api/v2/auth/`).
- 2026-05-07 (slice6-task9): `ReadwiseClient` + DTOs + DTO tests landed as commit `2a11887`. Pure scaffolding handled inline (TDD: red → green). Three files, 149 LOC total. **Plan-defect caught and fixed**: the plan's `ReadwiseDtosTest` snippet specified `Json { encodeDefaults = false; explicitNulls = false }` AND asserted `"\"source_type\":\"podcast\""` appears in the wire — but `sourceType = "podcast"` is the default value, so `encodeDefaults = false` strips it. Reconciled toward the assertion (which encodes the actual wire-shape contract Readwise expects): set `encodeDefaults = true` in the test JSON config. The second test (`nullableFieldsOmittedWhenAbsent`) still passes because `explicitNulls = false` is what suppresses the nullable defaults — `encodeDefaults` is orthogonal to nullable-omission. ktlint also reformatted `ReadwiseClient`'s 2- and 3-arg method signatures to one-param-per-line — kept. HTTP not unit-tested at this layer per plan (covered by `ReadwiseSinkTest` in Task 10 with a MockEngine). Green-check: `compileFossDebugKotlinAndroid` ✅, `compileKotlinIosSimulatorArm64` ✅, `ktlintCheck` ✅, `detekt` ✅, `testFossDebugUnitTest --tests ReadwiseDtosTest` 2/2 ✅. Next: Task 10 (`ReadwiseSink` + tests — first sink with retry/error mapping, exercises `ReadwiseClient` over Ktor `MockEngine`).
- 2026-05-07 (slice6-task10): `ReadwiseSink` + 4 tests + `open` modifiers on `ReadwiseClient` landed across two commits: `9fbf17f` (initial drop) and `edd3ca9` (follow-up: tighten test payload assertions). **Two plan-defects reconciled in the implementer dispatch**: (1) the plan's `FakeVault : OAuthTokenVault()` had stale class-shape syntax — `OAuthTokenVault` is an `interface` since Task 4 (`33c27e0`), so changed to `: OAuthTokenVault` (no parens) with explicit `override` on the three suspend methods; (2) the plan's `ReadwiseClient(io.ktor.client.HttpClient())` no-arg HttpClient construction would fail in commonTest (no engine wired) — moved test to `androidUnitTest` (matches `GeminiClientTest` pattern; `ktor-client-mock` lives only in `androidUnitTest`) and used `HttpClient(MockEngine) { engine { addHandler { respond("", HttpStatusCode.OK) } } }` to satisfy the super-call. Spec reviewer (haiku, ✅ no findings). Code-quality reviewer (feature-dev:code-reviewer, haiku) flagged: 2× Critical (test only checks call counts on the fake, not the request payload — could pass with wrong body or missing `kofipodId`); 1× High (Long → String → Long round-trip on `externalId` is "fragile"); 1× Important (dead MockEngine handler in fake). Critical findings accepted and fixed in `edd3ca9` — `FakeReadwiseClient` now captures `lastCreateRequest` / `lastUpdateRequest`, and the two happy-path tests assert text/title/author/sourceUrl/note/sourceType on the captured payload. High dismissed as a false positive — `ExportLog.externalId` is a `TEXT` column shared across sinks (Obsidian filename, Notion UUID, Readwise Long), so the String boundary is correct by design and `priorExternalId.toLongOrNull()` is the appropriate re-entry validation. Important dismissed as cosmetic — `MockEngine` requires *some* handler config to satisfy construction; the addHandler is structural, not dead in the way that matters. Green-check: target tests 4/4 ✅, ktlintCheck ✅, detekt ✅, `compileKotlinIosSimulatorArm64` ✅. Next: Task 11 (`SinkRegistry` + new `PkmExportCoordinator.execute(request, destination)` + tests — wires the sinks together and deletes the obsolete Slice 5 `PkmExportSink`/`MarkdownSink`/`MarkdownExporter` types).
- 2026-05-07 (slice6-task11): `SinkRegistry` + new 8-arg `PkmExportCoordinator` + 4 Slice-6 coordinator tests + 11 migrated Slice-5 tests + `PkmExportScheduler` interface + DI rewire landed across three commits: `dc90171` (initial), `3f21cf0` (spec-review fixes), `5531fbe` (code-quality fixes). **Five plan-defects reconciled in the implementer dispatch**: (1) `PkmExportSink.kt` is not a separate file — the enum lives inside `PkmExportRequest.kt`, so the implementer edited that file rather than `rm`-ing a non-existent path; (2) `PkmExportScheduler` was specified as `expect class` in Task 12 but Task 11's coordinator references it — established the recurring KMP `interface + expect class Impl` pattern up front by creating a plain `interface PkmExportScheduler { fun enqueue() }` in commonMain (Task 12 will add `expect class PkmExportSchedulerImpl` actuals on top); (3) `CommonModule.kt` DI block had to be rewired comprehensively (remove `MarkdownExporter`/`MarkdownSink`, add `ClipboardSink`/`ShareFileSink`/`ObsidianSink`/`ReadwiseSink`/`ReadwiseClient`/`SinkRegistry`/`PkmConnectionRepository`/stub `PkmExportScheduler`, update coordinator to 8-arg shape); (4) two `ExportSink` singles disambiguated by binding as concrete types (`ClipboardSink`/`ShareFileSink`) rather than via Koin named qualifiers; (5) `MarkdownExportSheet.kt` migrated `PkmExportSink.X` → `PkmDestination.X` (Obsidian/Readwise rows are deferred to Task 14's `ExportActionSheet` per spec — TODO comment added). Spec reviewer (sonnet) flagged 2× Blocker: (a) item-kind constant for `AiSummary` was `"ai_summary"` but plan wire-format says `"summary"` — fixed in `3f21cf0`; (b) `successWritesExportLogAndEmitsResult` test did not assert the emitted result variant, leaving the new `PkmExportResult.Exported` contract unpinned — fixed in `3f21cf0` by adding emit-side assertions to all 4 Slice-6 tests. **Plan-amendment accepted, not reverted**: the implementer added `data object Exported : PkmExportResult` (the plan said connection-bound success should emit `Shared`). Rationale: `Shared` semantically means "system share sheet was invoked" (which has its own UI signal), while connection-bound success needs an explicit snackbar since there is no system UI. CLAUDE.md "business requirements supersede review feedback" supports the divergence; documented with kdoc on `Exported`. `ExportLogRepository` refactored to interface + `Impl` to enable `FakeExportLog` test fakes — accepted as testability-required (mirrors Task 4's `OAuthTokenVault` interface refactor and Task 8's `ObsidianFolderWriter` interface refactor). Code-quality reviewer (feature-dev:code-reviewer, sonnet) flagged 1× Important + 1× Minor: (a) `catch (t: Throwable)` swallowed `CancellationException` and broke structured concurrency — fixed in `5531fbe` by re-throwing `CancellationException` before the generic handler; (b) stale `MarkdownExportSheet.kt` kdoc claimed coordinator clears via `finally` block — fixed in `5531fbe` to accurately say "after the export coroutine returns". Reviewer's Minor #3 (FakeDeps duplication between Slice5 and Slice6 test files) deferred — will become a stronger DRY argument once Task 12 adds a third file. Reviewer's Minor #4 (sheet doesn't render Obsidian/Readwise rows) was intentional Task-14 deferral; TODO comment added in `5531fbe`. Green-check at `5531fbe`: PKM tests 38 tasks BUILD SUCCESSFUL, `compileFossDebugKotlinAndroid` ✅, ktlintFormat ✅, detekt ✅, `compileKotlinIosSimulatorArm64` ✅. Next: Task 12 (`PkmExportWorker` Android + `PkmExportScheduler` expect/actual class — replace the Task-11 stub with WorkManager-backed scheduler; the worker calls `coordinator.retry(entry)` for queued/failed rows from `ExportLogRepository.selectQueuedOrFailed()`).
- 2026-05-07 (slice6-task12): `PkmExportWorker` + `AndroidPkmExportScheduler` + `IosPkmExportScheduler` + DI rewire landed as commit `bc39640`. **One plan-defect reconciled inline**: the plan specified `expect class PkmExportScheduler` (Task 12 step 1) but Task 11 already established `interface PkmExportScheduler` in commonMain (per the recurring KMP "interface + per-platform class" pattern that `AiSummaryScheduler` uses). Followed the existing codebase convention — `interface PkmExportScheduler` in commonMain + concrete `AndroidPkmExportScheduler(context)` in androidMain over WorkManager + concrete `IosPkmExportScheduler` no-op in iosMain — rather than introducing a redundant `expect class Impl` ceremony for a single one-method seam. DI binding moved from CommonModule (Task-11 stub) to platform modules: AndroidModule binds `AndroidPkmExportScheduler(androidContext())`, IosPlatformModule binds `IosPkmExportScheduler()`. Code-quality reviewer (feature-dev:code-reviewer, haiku) flagged 1× Critical + 1× Important: (a) Critical — `runCatching { coordinator.retry(entry) }` swallows `CancellationException`, breaking structured concurrency on WorkManager cancel; fixed by replacing with explicit `try/catch (CancellationException) { throw }` re-throw before the generic handler. Same defect class as Task 11's `executeInternal` (catch 5531fbe). (b) Important — `ExistingWorkPolicy.APPEND_OR_REPLACE` differs from the proven `AndroidAiSummaryScheduler` (`KEEP`) without justification, and is unnecessary because the worker reads queued rows from `ExportLog` (so a single queued fire drains all of them); switched to `KEEP` and updated kdoc accordingly. Both fixes landed in the same commit before push. Worker is auto-discovered by WorkManager via reflection — no `AndroidManifest.xml` registration needed (verified against `AiSummaryWorker`). No unit test for `PkmExportWorker` because the existing pattern (`AiSummaryWorker`) doesn't unit-test workers — they're tightly coupled to `CoroutineWorker` + Koin and the only branch (`status != STATUS_QUEUED` skip) is mechanical. Green-check: `compileFossDebugKotlinAndroid` ✅, `compileKotlinIosSimulatorArm64` ✅, `ktlintCheck` ✅, `detekt` ✅, `testFossDebugUnitTest` ✅. Next: Task 13 (Koin wiring — `CommonModule.kt` + `AndroidModule.kt` + `IosPlatformModule.kt` — actually most of Task 13 is already done as a side effect of Task 12; remainder is wiring sinks/repository singletons that were stubbed earlier and any final imports).
- 2026-05-07 (slice6-task13): iOS Koin parity for `OAuthTokenVault` and `ObsidianFolderWriterImpl` landed as commit `28208ac`. **Plan-defect reconciled inline**: the plan's Step 1 included a `viewModel { ConnectionsViewModel(...) }` binding in `CommonModule.kt`, but `ConnectionsViewModel` doesn't exist yet — it lands in Task 14. Deferred that single binding to Task 14 (the binding belongs alongside the class definition); the rest of the plan's Common/Android wiring was already in place from Tasks 11 + 12 side effects. Verified by reading all three Koin modules: `CommonModule.kt` already has all Slice 6 bindings (PkmConnectionRepository, ExportLogRepository, ClipboardSink, ShareFileSink, ReadwiseClient, ObsidianSink, ReadwiseSink, SinkRegistry, PkmExportCoordinator); `AndroidModule.kt` already has the platform-context-bound siblings (OAuthTokenVaultImpl as interface, ObsidianFolderWriterImpl, AndroidPkmExportScheduler). The only gap was `IosPlatformModule.kt` — needed `OAuthTokenVault` interface binding + `ObsidianFolderWriterImpl` concrete binding for graph parity (iOS doesn't `startKoin` at runtime today, but parity preserves compile-time correctness when iOS bootstrap lands). No subagent dispatch — this was a 7-line append plus a green-check sequence (zero new logic; `compileFossDebugKotlinAndroid` ✅, `compileKotlinIosSimulatorArm64` ✅, `ktlintCheck` ✅, `detekt` ✅, `testFossDebugUnitTest` ✅). Next: Task 14 (`Route.Connections` + `ConnectionsViewModel` + `ConnectionsScreen` — the first UI surface of Slice 6; will land the deferred ViewModel Koin binding).
- 2026-05-07 (slice6-task14): `Route.Connections` + `ConnectionsViewModel` + `ConnectionsScreen` + `ObsidianFolderPickerLauncher` (expect/actual) + `ConnectionsViewModelTest` (3 tests) + the deferred Koin viewModel binding from Task 13 landed across two commits: `4ba8f78` (initial drop including 3 self-review fixes) + `03455b4` (code-quality review fixes). **Six plan-defects reconciled in the implementer dispatch**: (1) plan snippet omitted `: ViewModel()` extension on the VM — every Kofipod VM does this for Koin's `viewModel { }` factory (matches `SearchViewModel`); added. (2) plan listed `entitlement: ProEntitlementRepository` in the constructor but never referenced it in any method — Pro-gating happens at the Settings entry row in Task 15, not here; omitted (YAGNI). (3) plan's `connections.observeAll()` direct dependency on `PkmConnectionRepository` doesn't allow unit-testing (`withContext(Dispatchers.Default)` internals + non-open class); introduced `interface ConnectionsSource` + `class PkmConnectionsSource` adapter — same `interface + Impl` pattern used at Task 4 (OAuthTokenVault), Task 8 (ObsidianFolderWriter), Task 11 (ExportLogRepository). (4) `Clock` made a constructor default (`= Clock.System`) rather than DI-bound — matches `BookmarksViewModel` (no other VM injects Clock via DI). (5) tests landed in `androidUnitTest` not `commonTest` because `MockEngine`/`HttpClient` are JVM-only — same as Task 10's `ReadwiseSinkTest`. (6) implementer initially missed plan's `.filter { it != ConnectionKind.Notion }` directive; caught in self-review and fixed in `4ba8f78`. **`4ba8f78` self-review fixes (3)**: Notion filter; SAF picker drops URI when `takePersistableUriPermission` fails (so `SecurityException` doesn't strand a broken row in the DB across restarts); Readwise's `.detail` no longer leaks the `"readwise.token"` vault key into the subtitle. **`03455b4` code-quality reviewer fixes (3)**: (a) Critical — `runCatching { readwiseClient.verify(token) }.getOrElse { false }` swallowed `CancellationException` (same defect class as Task 11's `executeInternal` catch and Task 12's `runCatching { coordinator.retry(entry) }`); fixed by adding `.onFailure { if (it is CancellationException) throw it }` before `.getOrElse`. (b) High — fire-and-forget validation `Job` could clobber state if user closed dialog mid-validation then reopened; tracked the launched Job in `validateJob` field and cancel it in `closeReadwiseDialog()`. (c) High — line 166's `Connected(detail ?: "Connected")` re-introduced the "…/Connected" subtitle bug for Readwise that the prior round was supposed to fix; dropped the fallback to pass `null` (sealed class already accepts nullable; `rowSubtitle` falls through to "Last synced" / `null` correctly). Plus a new `closeReadwiseDialogCancelsInflightValidate` test using `awaitCancellation()` in `FakeReadwiseClient.verify` to pin the cancel behaviour. Final state: 3/3 tests pass, ktlint + detekt + iOS compile + Android compile + full foss-debug test suite all green. Next: Task 15 (Settings → Connections entry row — Pro-gated entry that opens `Route.Connections`).

## Notes

- Slice 6 task progress: Tasks 0–14 ✅ done. **Next: Task 15** (Settings → Connections entry row — Pro-gated row in Settings that opens `Route.Connections`). Plan tasks 1–11 are data + adapter layer (TDD-heavy), 12–16 are UI wiring, 17 is the green-check + close-out commit.
- **Plan-defect log**:
  - Task 9: self-contradictory `Json` config (`encodeDefaults = false` vs `"source_type":"podcast"` assertion). Followed the assertion (= wire-shape contract) and set `encodeDefaults = true`.
  - Task 10: (a) `FakeVault : OAuthTokenVault()` — stale class-shape syntax; `OAuthTokenVault` is an `interface` since Task 4. Fixed to `: OAuthTokenVault` with `override` on the three methods. (b) Test placed in `commonTest` but uses `HttpClient()` no-arg construction with no engine on classpath. Moved to `androidUnitTest` (where `ktor-client-mock` is wired) and used `HttpClient(MockEngine) {...}` to satisfy the fake's super-call.
  - Task 11: (a) `PkmExportSink.kt` is not a separate file — the enum lives inside `PkmExportRequest.kt`. Edited that file rather than `rm`-ing a non-existent path. (b) `PkmExportScheduler` was specified as `expect class` in Task 12 but Task 11 references it — created plain `interface PkmExportScheduler` in commonMain up front (`interface + expect class Impl` pattern); Task 12 follows up with the expect class. (c) DI rewire wider than the literal plan: `MarkdownExporter`/`MarkdownSink` removed; `ClipboardSink`/`ShareFileSink`/`ObsidianSink`/`ReadwiseSink`/`ReadwiseClient`/`SinkRegistry`/`PkmConnectionRepository`/stub `PkmExportScheduler` added; `AndroidModule.kt` got `OAuthTokenVaultImpl` + `ObsidianFolderWriterImpl` (deferred from Tasks 3 + 8). (d) Two `ExportSink` singles disambiguated by binding as concrete types (`ClipboardSink`/`ShareFileSink`) — no Koin named qualifiers. (e) Item-kind constant for `AiSummary` initially used `"ai_summary"` but plan wire-format says `"summary"` — fixed before close-out (caught by spec reviewer; bug would have silently dropped AI-summary retries). (f) Plan said connection-bound success emits `PkmExportResult.Shared`, but `Shared` semantically means "system share sheet" (which has its own UI signal). Added `data object Exported : PkmExportResult` for connection-bound destinations (Obsidian / Readwise / Notion) since they need an explicit snackbar — accepted as plan-amendment per CLAUDE.md "business requirements supersede review feedback". Documented with kdoc on `Exported`. (g) `ExportLogRepository` refactored to interface + `Impl` to enable `FakeExportLog` test fakes — same pattern as `OAuthTokenVault` (Task 4) and `ObsidianFolderWriter` (Task 8). (h) `executeInternal` initially caught `Throwable` without re-throwing `CancellationException`, breaking structured concurrency on appScope teardown — fixed before close-out (caught by code-quality reviewer).
  - Watch for similar copy-paste defects in remaining task snippets — especially the `OAuthTokenVault()` open-class shape (Tasks 13–14 `ReadwiseConnectViewModel` may have the same staleness). Tasks 13+ should use the established `interface + expect class Impl` shape directly when introducing platform deps.
  - Task 12: (a) plan said `expect class PkmExportScheduler` but Task 11 already established `interface PkmExportScheduler` in commonMain. Followed `AiSummaryScheduler`'s plain interface + per-platform concrete class pattern instead — cleaner than `expect class Impl` for a one-method seam. (b) `runCatching { coordinator.retry(entry) }` would swallow `CancellationException` (same defect class as Task 11's `executeInternal` Throwable catch); fixed before close-out by switching to explicit `try/catch (CancellationException) { throw }` re-throw. (c) Plan-snippet's `ExistingWorkPolicy.APPEND_OR_REPLACE` was a misfit — the worker reads queued rows from the DB so a single queued fire drains them all; switched to `KEEP` mirroring `AndroidAiSummaryScheduler` per code-quality reviewer.
  - Task 13: plan included `viewModel { ConnectionsViewModel(...) }` in `CommonModule.kt` Step 1, but `ConnectionsViewModel` is introduced in Task 14 — class doesn't exist yet, so the binding would not compile. Deferred the ViewModel binding to Task 14 (which lands the class). The rest of Task 13's CommonModule + AndroidModule wiring was already in place from Task 11/12 side effects; only iOS module needed the two missing bindings (`OAuthTokenVault` interface, `ObsidianFolderWriterImpl`).
  - Task 14: (a) plan snippet omitted `: ViewModel()` extension; required for Koin's `viewModel { }` factory (matches `SearchViewModel`); added. (b) plan's `entitlement: ProEntitlementRepository` constructor param was never referenced in any method; omitted per YAGNI. (c) plan's `connections.observeAll()` directly against `PkmConnectionRepository` was untestable (non-open + `withContext(Dispatchers.Default)` internals); introduced `interface ConnectionsSource` + `PkmConnectionsSource` adapter — same `interface + Impl` pattern as Tasks 4/8/11. (d) `Clock` made a constructor default (`= Clock.System`) — matches `BookmarksViewModel`. (e) tests live in `androidUnitTest` not `commonTest` because `MockEngine`/`HttpClient` are JVM-only — same as Task 10's `ReadwiseSinkTest`. (f) implementer initially missed plan's `.filter { it != ConnectionKind.Notion }` directive; caught in self-review and fixed before commit. (g) recurring `runCatching` swallowing `CancellationException` defect (third occurrence after Tasks 11 + 12); fixed in `03455b4`. (h) "…/Connected" subtitle leak — first round's fix to `val detail` was incomplete; line 166's `Connected(detail ?: "Connected")` still re-introduced the same bug; fixed by passing nullable through directly. (i) fire-and-forget validation `Job` could clobber state if dialog closed mid-validation then reopened; tracked Job in field + cancel in `closeReadwiseDialog()`.
- **Recurring KMP gotcha (Task 4 + Task 8)**: when a platform actual needs a constructor parameter (e.g. Android `Context`) but the expect declared a no-arg primary, the actual cannot match. Resolution is `interface X` (commonMain) + `expect class XImpl : X` with the actual's primary taking the platform deps. Watch for this in any future expect/actual seam that needs DI on Android. Future task plans should write the `interface + expect class Impl` shape directly rather than the `expect open class` shape that triggers this.
- **Resolved (was deferred from Task 4)**: `ExportLog` has no FK to `PkmConnection` (composite PK only), so the FK-off `inMemoryDatabase()` helper is sufficient. The FK-enforcement footgun (`PRAGMA foreign_keys = ON` not set in `inMemoryDatabase()` only in `inMemoryDatabaseWithDriver()`) remains for any future repo whose schema does have FKs — promote into the default helper if/when it bites.
- Slice 5 verified on 2026-05-06 with the full green-check sequence (compile Android + ktlint + detekt + foss debug unit tests + iOS simulator compile + Paparazzi verify). Auto Backup / file-domain caveat from CLAUDE.md still applies to any new tables Slice 6 introduces (PkmConnection, ExportLog) — they will ride along with `KofipodDatabase` automatically since the entire DB file is in the included `database` domain.
- Slice 6 will introduce `ExportLog` and `PkmConnection` tables (spec § "Schema additions" lists them under Slice 5 in the table, but the Slice 5 plan deferred them — this is reconciled by the slice 5 plan's self-review). Schema version after Slice 6 should land at **19** per the spec table (collapsing PkmConnection + ExportLog into a single migration when added).
- Per recent commit `e1487dc` the Family SKU was dropped and Smart Playlists were promoted into v1.0 — so v1.0 launches schema **19** (covering PkmConnection + ExportLog + SmartPlaylist if SmartPlaylist also lands pre-launch). Spec slice table still says SmartPlaylist is migration after Slice 7 (would be schema 20) — the slice ordering needs to be verified against spec when Slice 7 plan is authored.
- Build flavor reminder: post-Slice-0, the unit test task is `:composeApp:testFossDebugUnitTest` (not `testDebugUnitTest`, which became ambiguous after the flavor split — see commit `fa80659`). The Paparazzi verify task is `:composeApp:verifyPaparazziFossDebug` for the same reason.
- Slice 6 spec considerations (re-read before plan authoring): Readwise auth (OAuth vs API-token paste — the spec offers both, plan should commit to one); Obsidian SAF persistence requires `takePersistableUriPermission`; ExportLog idempotency key shape; how `PkmExportWorker` interacts with the existing `appScope` collectors and the AI Summary worker — make sure they don't fight for `WorkManager` constraints.

## Blockers

(none)
