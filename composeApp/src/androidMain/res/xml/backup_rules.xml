<?xml version="1.0" encoding="utf-8"?>
<!--
  Auto Backup rules for Android 12+ (API 31+).

  cloud-backup: what gets uploaded to the user's Google Drive backup
  (transparent, free, doesn't count against Drive quota, ~25 MB cap per app).

  device-transfer: what gets copied during direct device-to-device transfer
  (cable / Quick Start). Same set as cloud — both restore the library.

  Included: SQLDelight database (subscriptions, lists, episode metadata,
  playback state, settings) and any SharedPreferences. Together this is
  well under 1 MB.

  Not included: downloaded audio under files/downloads/, the streaming
  playback cache under cache/media/, and the in-app updater's downloaded
  APK under files/updates/. Those live in domains we never <include>, so
  Auto Backup skips them by default.

  Explicitly excluded:
    * `kofipod_local` — device-local pointers (currently the downloaded-APK
      path) that must match what's actually on disk. Restoring onto a new
      device would crash the installer when it tried to open a missing file.
    * `kofipod_secure` — encrypted store for the user's BYOK secrets: the
      Gemini API key and (FOSS builds) the Podcast Index API key + secret.
      These are per-device by design and must not sync; restoring them onto
      another phone would expose the user's quota to a device they no longer
      control. The user re-pastes their key(s) on the new device if they want
      AI features / FOSS search there.
    * `kofipod_entitlement` — last verified Pro entitlement reading. Excluded
      so a device clone / restore cannot resurrect a stale "Pro" state. Real
      entitlement re-verifies via Play Billing on every cold start.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
        <exclude domain="sharedpref" path="kofipod_local.xml" />
        <exclude domain="sharedpref" path="kofipod_secure.xml" />
        <exclude domain="sharedpref" path="kofipod_entitlement.xml" />
    </cloud-backup>
    <device-transfer>
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
        <exclude domain="sharedpref" path="kofipod_local.xml" />
        <exclude domain="sharedpref" path="kofipod_secure.xml" />
        <exclude domain="sharedpref" path="kofipod_entitlement.xml" />
    </device-transfer>
</data-extraction-rules>
